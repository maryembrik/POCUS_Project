% =========================================================================================
% Chapter 6 -- Clinical Reasoning Agent
%
% Numbers here were read from the code and from models/clinical_reasoning_rag_ab/.
% Regenerate the A/B figures with notebooks/clinical_reasoning_rag_ab.ipynb.
% =========================================================================================
\section{Modeling: Clinical Reasoning Agent}
\label{sec:reasoning}

\subsection{Introduction}

The perception modules described in Chapter~\ref{sec:perception} answer questions about
images. A clinician asks a different question: what is wrong with this patient, and what
should be done next. No combination of image classifiers answers that, because the answer
also depends on the vitals, the laboratory results and the history, none of which the
imaging models can see.

This chapter describes the layer that brings those pieces together. Its design can be put
in one sentence: the agent separates safety-critical decisions from generative reasoning.
Whether a case is escalated is determined by ordinary code reading the structured record,
while retrieval and the language model are responsible only for producing a differential
and explaining the evidence behind it. Fig.~\ref{fig:pipeline} shows the two as separate
branches that meet only at the output.

Underneath that separation sits one organising idea.

\begin{keystatement}
Absent is not normal. A test that was never performed cannot support or contradict any
diagnosis, and a model with no healthy class can never exclude disease.
\end{keystatement}

\subsection{The Clinical State}
\label{subsec:clinical-state}

Everything the agent knows about a patient is collected into a single structured record
called the clinical state. It holds three kinds of information: the imaging findings, the
measured values, and --- importantly --- what is missing.

Imaging findings are recorded in three states rather than two. A finding may be
\emph{detected}, meaning the module looked and saw it; \emph{not detected}, meaning the
module looked and did not see it; or simply absent from the record, meaning nobody looked.
The middle case is a real observation and the last one is not. Collapsing them would throw
away information in one direction and invent it in the other.

Vitals and laboratory values are stored with their reference ranges, and each is flagged
high, low or normal by the code before the model sees it. A value that was not measured is
listed in a separate block, printed under the heading \texttt{NOT MEASURED --- absent, NOT
normal}. Tab.~\ref{tab:statefields} summarises the record.

\begin{table}[H]
  \centering
  \small
  \begin{tabular}{lp{9.2cm}}
    \toprule
    \textbf{Part of the state} & \textbf{What it holds} \\
    \midrule
    Imaging findings   & label, confidence, detected or not detected, evidence grade \\
    Organs not assessed& organs that were requested but never scanned \\
    Vitals             & value, reference range, high/low/normal flag \\
    Laboratory values  & value, reference range, high/low/normal flag \\
    Missing            & every vital and laboratory value that was not measured \\
    Conflicts          & disagreements between the triage and imaging agents \\
    Case quality       & an overall grade with the reasons that produced it \\
    \bottomrule
  \end{tabular}
  \caption{The clinical state passed to the reasoning layer.}
  \label{tab:statefields}
\end{table}

The two perception agents produce differently shaped outputs --- the triage agent gives one
urgency tier with a confidence, the ultrasound agent gives a list of findings --- and both
are normalised into this single form. Each finding also receives an evidence grade:
\emph{experimental} when the module's confidence is not calibrated, \emph{limited} when the
confidence is low or the training evidence was thin, and \emph{strong} otherwise. The grade
travels with the finding so that later decisions can take account of how much the number is
worth.

\subsection{Conflict Detection}
\label{subsec:reasoning-conflict}

The two agents can disagree. The clearest example is a patient triaged as low urgency whose
scan shows a severe finding with high confidence. The system flags this and does not try to
settle it. It has no information that would say which source is wrong, so averaging the two
or preferring the more confident one would be a guess presented as a judgement. The
disagreement is reported and the case is escalated.

\subsubsection{A Second Kind of Disagreement}
\label{subsec:reasoning-human-ai}

The conflict above is between two of the system's own agents. Once the triage tier is proposed
by a model and confirmed or changed by a clinician
(Section~\ref{subsec:triage-deployment}), a second kind becomes observable: two independent
assessments of the same patient, one of them human, that did not coincide.

It is recorded with the encounter --- what was suggested, what the clinician settled on, and in
which direction they differ. Three properties of that record are deliberate, and each is
enforced by a test.

\begin{keystatement}
The system detects that two assessments differ. It does not conclude that either is wrong,
because it has no ground truth with which to decide.
\end{keystatement}

\paragraph{It does not change the tier.} The clinician's assessment is what reaches the
reasoning layer, before this comparison is computed and after it. Were the recorded tier ever
to become the model's, the control on the intake screen would be advisory rather than
decisive, and every rule reading the tier --- the conflict detector above among them --- would
be reading a number the clinician did not choose.

\paragraph{It does not escalate.} This is the least obvious of the three and the one most worth
stating. Escalation is a judgement about the \emph{patient}, made from evidence about the
patient; a disagreement between two assessors is a fact about the \emph{assessment}. Connecting
it to the escalation policy would mean a model that graded a case higher than the clinician
could raise its urgency indirectly --- taking, through a side channel, the authority the design
gives the clinician. A test compares the trigger list for a disagreeing encounter against the
identical encounter with no suggestion at all, and requires them to be the same.

\paragraph{The direction is recorded, because the two are not equivalent.} A clinician grading
\emph{above} the suggestion is being more cautious than the model, which warrants no attention.
Grading \emph{below} it is the direction in which a missed deterioration would sit. That is
worth reviewing --- afterwards, across many encounters, by a person --- and the record exists so
that it can be. It is not a signal to act on during the consultation, and it is not shown as
one: it lives in the archived report, not on the assessment screen.

Taken with the other two, the system now distinguishes three kinds of uncertainty rather than
conflating them: uncertainty about a \emph{finding} (a module reporting evidence too thin to
act on), uncertainty about the \emph{record} (a measurement never taken, an organ never
examined, a finding outside a model's scope), and uncertainty about the \emph{assessment}
itself.

\subsection{Escalation}
\label{subsec:reasoning-escalation}

Whether a case is answered directly or escalated is decided before the language model runs,
by the rules in Tab.~\ref{tab:escalation}. Each rule reads only the structured state. None
of them depends on the model having noticed anything, so the escalation behaviour is the
same regardless of which model is used, or whether the model fails entirely.

\begin{table}[H]
  \centering
  \small
  \begin{tabular}{p{10.5cm}}
    \toprule
    \textbf{The case is escalated when\ldots} \\
    \midrule
    the two agents disagree \\
    the overall case quality is graded poor \\
    a high-risk finding rests on evidence that is not strong \\
    a high-risk finding is below the confidence considered decisive (0.80) \\
    imaging is positive but a key laboratory value (troponin, lactate) was not measured \\
    nothing was found, but the models cannot rule out disease \\
    an organ was requested and never assessed \\
    \bottomrule
  \end{tabular}
  \caption{Escalation rules. All are evaluated on the structured state before generation.}
  \label{tab:escalation}
\end{table}

The last rule deserves a note. It applies whether or not something was found elsewhere. A
finding in the lung says nothing about a heart nobody scanned, and a positive result in one
organ raises the importance of the gap rather than closing it.

\subsection{Knowledge Corpus}
\label{subsec:reasoning-kb}

To let the agent consult reference material rather than rely only on what the model
remembers, a small corpus of thirty knowledge units was assembled. Each unit answers one
clinical question and carries the citation it came from. Tab.~\ref{tab:corpus} shows the
composition.

\begin{table}[H]
  \centering
  \small
  \begin{tabular}{llr}
    \toprule
    \textbf{Group} & \textbf{Covers} & \textbf{Units} \\
    \midrule
    Lung       & B-lines, A-lines, lung sliding, pneumothorax, BLUE protocol & 10 \\
    Cardiac    & left and right ventricle, pericardial effusion, tamponade   & 5 \\
    FAST       & the four trauma views and their interpretation              & 5 \\
    Shock      & classification and the four shock categories                & 5 \\
    Boundaries & what a focused study does not establish                     & 5 \\
    \bottomrule
  \end{tabular}
  \caption{The thirty knowledge units, by group.}
  \label{tab:corpus}
\end{table}

All thirty are drawn from six open-access papers under Creative Commons licences, so the
text can be redistributed with the project. Two constraints shaped what went in. First,
laboratory reference ranges were deliberately kept out: comparing a value against a
threshold is a fixed calculation the code already performs, and putting the same
information in a retrieval corpus would make a settled comparison depend on a similarity
score. Second, passages were chosen for the limits they state rather than for definitions.
The most useful units are those recording what a test cannot settle --- that the lung point
sign is only 66\% sensitive, that lung sliding is absent in eight conditions besides
pneumothorax, or that a negative FAST detects free fluid reliably only above
150--200\,mL.

The corpus does not cover obstetric, vascular or paediatric ultrasound, and it is not a
substitute for a guideline. It is small by design: enough to test whether retrieval helps,
not enough to claim coverage of emergency ultrasound.

\subsection{Retrieval}
\label{subsec:reasoning-rag}

Retrieval uses TF--IDF over the thirty units. For a corpus of this size it performs
comparably to a neural retriever, needs no model download, and its scores can be inspected,
which matters more here than a small gain in ranking.

Two rules govern how results are used. A passage below a relevance threshold is reported as
no result at all, because handing the model plausible text that does not bear on the case
creates a new opportunity for unsupported claims. And any citation the model produces must
correspond to a passage that was actually retrieved. If nothing was retrieved, the prompt
says so explicitly rather than staying silent, so the model cannot imply guideline support
it never had.

\subsection{The Language Model}
\label{subsec:reasoning-llm}

The model is HuatuoGPT-o1-8B, a medical model, run locally through \texttt{llama.cpp} in
4-bit quantised form (4.9\,GB). Running locally rather than through an API is a deliberate
choice: patient data does not leave the machine.

The cost is real. On a consumer CPU a single case takes roughly 23 minutes. On a Colab T4
the same case takes 40 to 90 seconds, and the full five-case benchmark runs in about five
minutes. Temperature is set to zero with a fixed seed, and the model's cache is cleared
between calls, so repeated runs give identical output.

\subsection{The Pipeline}
\label{subsec:reasoning-pipeline}

Fig.~\ref{fig:pipeline} shows how the parts fit together. The three inputs are merged into
the clinical state, and from there the pipeline splits. One branch decides escalation using
the rules of Tab.~\ref{tab:escalation}. The other branch retrieves reference passages,
assembles the prompt, generates an answer and checks it. The two branches meet only at the
output.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      node distance = 6mm,
      font = \small,
      box/.style   = {draw, rounded corners=2pt, align=center, inner sep=4pt,
                      minimum height=7mm},
      inp/.style   = {box, fill=black!4, minimum width=26mm},
      state/.style = {box, fill=black!8, align=left, text width=58mm},
      safe/.style  = {box, fill=black!12, very thick, align=center, text width=40mm},
      gen/.style   = {box, align=center, text width=40mm},
      % Not `out`: that is a built-in TikZ key (the outgoing angle of a `to` path), so a
      % style of the same name is read as that key with no value and the picture dies.
      outbox/.style = {box, align=center, text width=38mm},
      ar/.style    = {-{Latex[length=2mm]}, thick},
    ]

    % ---- inputs -----------------------------------------------------------------
    \node[inp] (triage) {Triage Agent};
    \node[inp, below=of triage] (us) {Ultrasound Agent};
    \node[inp, below=of us] (labs) {Labs and vitals};

    % ---- clinical state ---------------------------------------------------------
    \node[state, right=14mm of us] (state) {%
      \textbf{Clinical state}\\[2pt]
      findings (detected / not detected)\\
      organs never assessed\\
      values with reference ranges\\
      \textbf{missing} information\\
      conflicts, case quality};

    \draw[ar] (triage.east) -- ++(4mm,0) |- (state.west);
    \draw[ar] (us.east) -- (state.west);
    \draw[ar] (labs.east) -- ++(4mm,0) |- (state.west);

    % ---- the two branches -------------------------------------------------------
    \node[safe, below left=12mm and -14mm of state] (esc) {%
      \textbf{Escalation decision}\\ seven deterministic rules\\ \emph{no model involved}};
    \node[gen, below right=12mm and -14mm of state] (rag) {%
      \textbf{Retrieval}\\ TF--IDF, 32 units};

    \draw[ar] (state.south) -- ++(0,-3mm) -| (esc.north);
    \draw[ar] (state.south) -- ++(0,-3mm) -| (rag.north);

    \node[gen, below=8mm of rag] (llm) {HuatuoGPT-o1-8B\\ local, \texttt{llama.cpp}};
    \node[safe, below=8mm of llm] (val) {\textbf{Validator}\\ answer checked against\\
      the clinical state};

    \draw[ar] (rag) -- (llm);
    \draw[ar] (llm) -- (val);

    % ---- revision loop ----------------------------------------------------------
    \draw[ar] (val.east) -- ++(5mm,0) |- node[pos=0.25, right=1mm, align=left, font=\tiny]
      {one correction\\ request, once} (llm.east);

    % ---- output -----------------------------------------------------------------
    \node[outbox, below=16mm of val] (final) {%
      \textbf{Output}\\ differential \emph{or} withheld\\ + escalation flag};

    \draw[ar] (val.south) -- node[right=1mm, font=\tiny, align=left]
      {deliver, or withhold\\ if still faulty} (final.north);
    \draw[ar] (esc.south) |- (final.west);

  \end{tikzpicture}
  \caption{The Clinical Reasoning Agent. Escalation is decided from the structured state
    alone and does not pass through the model, so it survives a model that fails, a
    retrieval that returns nothing, and an answer that is withheld.}
  \label{fig:pipeline}
\end{figure}

Separating the branches is the central design choice, and its value is easiest to see by
asking what happens when each part fails.

\begin{itemize}[nosep, leftmargin=*]
  \item \textbf{The model crashes or is unavailable.} The escalation decision and its
        triggers are produced unchanged; the differential is withheld and the backend error
        is recorded. This was run directly with a failing backend, not assumed.
  \item \textbf{Retrieval returns nothing.} The prompt says so explicitly, so the answer
        cannot imply support it never had. Escalation is unaffected.
  \item \textbf{The model invents evidence.} The validator withholds the differential.
        Escalation is still reported.
  \item \textbf{The model is replaced.} Escalation behaviour does not change, because no
        part of it reads the model's output.
\end{itemize}

None of this depends on the model behaving well, which is what distinguishes the design
from putting the same instructions in a prompt and hoping they are followed.

If the validator finds a serious fault, the answer is sent back once with a single
correction request. Sending one complaint at a time works better than sending several
together. If the revised answer is still faulty, it is withheld.

The output is a ranked list of diagnoses. Each carries a likelihood, the evidence for and
against it, and the limits of that evidence. Alongside it the agent reports what
information is missing, how confident it is, and a recommended next step. The system
recommends investigations; it does not prescribe treatment.

\subsection{Checking the Answer}
\label{subsec:reasoning-safety}

The model's answer is checked against the clinical state by a set of rules. Faults are
sorted into two levels. A \emph{serious} fault --- citing a test that was never performed,
inventing a finding, misreading a value, or using an unrelated negative as an argument
against a diagnosis --- causes the differential to be withheld entirely. A \emph{minor}
fault, such as untidy wording, is delivered with a warning attached.

Withholding rather than displaying with a caveat is a deliberate choice. A differential
that cites tests which were never run is not a slightly flawed answer; it is an answer built
on evidence that does not exist, and showing it beside a warning invites the reader to use
it anyway.

These rules are exercised by 335 automated tests, grouped by the property each one
protects. Tab.~\ref{tab:safetytests} lists the largest groups. The tests use scripted
responses and need no model, so they run in seconds and are independent of which model is
installed.

\begin{table}[H]
  \centering
  \small
  \begin{tabular}{lr}
    \toprule
    \textbf{Property} & \textbf{Tests} \\
    \midrule
    Hallucination rejection      & 21 \\
    Retrieval grounding          & 19 \\
    Absent is not normal         & 16 \\
    Malformed output rejection   & 15 \\
    Escalation policy            & 11 \\
    Conflict detection           & 9 \\
    Confidence calibration       & 9 \\
    Other properties (11 groups) & 58 \\
    \midrule
    \textbf{Total}               & \textbf{158} \\
    \bottomrule
  \end{tabular}
  \caption{Safety tests by property. All pass.}
  \label{tab:safetytests}
\end{table}

\subsection{Does Retrieval Help?}
\label{subsec:reasoning-ab}

Retrieval was added after the rest of the layer was working, and the version without it was
frozen so the two could be compared fairly. Five cases were run twice: once without
retrieval and once with it. Everything else was held constant.

Three checks confirm the comparison is sound. The no-retrieval run reproduced the frozen
version exactly on all five cases; the escalation decisions were identical in both runs, as
they must be since they are computed before the model; and repeated runs gave identical
output. Tab.~\ref{tab:ab} gives the result.

\begin{table}[H]
  \centering
  \small
  \begin{tabular}{llrrl}
    \toprule
    \textbf{Case} & \textbf{Passages found} & \textbf{Errors without} &
    \textbf{Errors with} & \textbf{Effect} \\
    \midrule
    missing      & 2 & 0 & 1 & worse \\
    conflict     & 1 & 1 & 1 & error replaced \\
    concordant   & 0 & 0 & 0 & no retrieval \\
    reassuring   & 0 & 1 & 1 & no retrieval \\
    not assessed & 3 & 4 & 2 & better \\
    \midrule
    \textbf{Total} & & \textbf{6} & \textbf{5} & \\
    \bottomrule
  \end{tabular}
  \caption{Validator errors with and without retrieval, on five designed cases.}
  \label{tab:ab}
\end{table}

The totals are almost unchanged, and the aggregate hides what actually happened. Retrieval
fixed two faults: a normal troponin that had been described as elevated, and an unexamined
organ that had been left out of the list of missing information. It also created a new
one.

\begin{keystatement}
Giving a small model reference text creates a new way for it to fabricate: it attributes
the reference's general statements to the specific patient.
\end{keystatement}

In two of the three cases where retrieval returned anything, the model took a sentence from
a retrieved passage and listed it as evidence about this patient. In one case it cited
``lung sliding (low specificity)'', a phrase from the pneumothorax unit. In another it
cited ``absence of sonographic signs of RV overload or dysfunction'', taken word for word
from the cardiac unit. Neither was an observation about the patient in front of it.

Every instance was caught by the checking rules and the differential was withheld. This is
the clearest evidence in the project for keeping the safety layer independent of the model:
retrieval opened a new way to fail, and the checks closed it without being changed.

Three limits apply to this experiment. Two of the five cases retrieved nothing, so they say
nothing about retrieval; the case with an entirely normal patient produces a query too thin
to match anything, which is a gap in how queries are built. The improvement on the third
case is partly an artefact, because the model changed its diagnosis rather than reasoning
better about the same one. And the corpus unit describing what a negative FAST misses does
not itself surface for that question, because TF--IDF matches words rather than meaning.

The conclusion is narrow and worth stating plainly: on these five cases, retrieval did not
make the system better. It changed which errors occurred.

\subsection{Decision Support and Automated Reporting}
\label{subsec:reasoning-decision-support}

A differential is not by itself a decision aid. The specification requires four further
outputs --- a severity level, critical alerts, recommendations for additional examinations,
and therapeutic suggestions in accordance with the recommendations --- together with an
automated clinical report. All five are implemented, and the design principle behind them is
the one that governs the rest of this chapter: \textbf{none of them is produced by the
language model}. The module that computes them imports nothing that can reach one.

The reason is not stylistic. \emph{Is this patient critical?} is a question whose answer is
already determined by values the structured state holds. Routing it through a quantised 8B
model would make a deterministic answer probabilistic, and would place a safety-critical
output inside the least predictable component of the system --- exactly the arrangement
Section~\ref{subsec:reasoning-escalation} exists to avoid.

\paragraph{Severity.} One level for the encounter, derived rather than judged, and
deliberately monotone: any single reason for concern raises it and nothing lowers it. A triage
tier of \emph{low} beside a critical alert therefore yields \textsc{high}. That combination is
precisely the disagreement the architecture exists to surface, and a rule that averaged the
two would bury the case the system was built for. Severity summarises the encounter for a
reader; it does not feed back into the escalation decision, which remains computed
independently and stays the routing authority.

\paragraph{Critical alerts.} Two kinds, and the second is what a simpler system omits.
\emph{Physiological} alerts fire when a measured value crosses a configured bound.
\emph{Structural} alerts fire on the state of the record itself: the agents disagreeing, a key
test never obtained, a modality the presentation expected and nobody performed. A patient can
be in danger because of what was not done, and no vital sign shows it.

Every numeric cutoff lives in one versioned configuration file rather than scattered through
the code, and every alert names the bound it fired on. A reader can therefore check an alert
against the configuration instead of trusting its label, and the generated report cites the
configuration version it used.

\paragraph{Recommended examinations.} Built from the record, not from the model's prose. An
examination reaches the list because a test was never performed, because an escalation trigger
names it, or because a modality the routed presentation expected is absent. The differential,
where available, is used only to raise priority --- a missing D-dimer matters more when
pulmonary embolism is under consideration --- and cannot add an examination that the record
does not already show to be missing. A hallucinated diagnosis therefore cannot produce a
hallucinated recommendation, and a test asserts it.

The system recommends an examination; it does not order one.

\paragraph{Therapeutic considerations.} This is the output that required the most restraint,
and the implemented behaviour is a deliberate refusal:

\begin{keystatement}
Therapeutic recommendations were deliberately gated behind retrieval of an authoritative,
citable protocol. In the absence of an appropriate protocol source, the system produces no
therapeutic recommendation rather than relying on the language model's parametric knowledge.
\end{keystatement}

The mechanism surfaces passages from a sourced protocol in the retrieval corpus, attributed to
it, framed as considerations requiring clinician verification rather than as instructions. When
no such protocol has been retrieved it returns nothing and states why. The corpus does not yet
contain a protocol source, so at the time of writing the layer is inert.

That is the correct behaviour rather than an incomplete one. A suggestion resting on the
model's training data would be indistinguishable, to a reader, from one backed by a protocol,
and the difference is exactly what a clinician would need to judge it. The same discipline
governs the corpus itself, where a passage marked \texttt{placeholder} can never pass as
evidence (Section~\ref{subsec:reasoning-rag}).

\paragraph{Scenario routing.} The four priority presentations of the specification --- acute
dyspnoea, undifferentiated shock, trauma, and cardiorespiratory arrest --- are routed from the
presenting complaint, with physiology as a fallback. Routing does not select a model and does
not suppress findings from organs outside the scenario; its purpose is to record which
modalities the presentation expected, so that one nobody performed is reported as a gap rather
than passing unnoticed. A case matching no scenario is reported as unclassified rather than
forced into the nearest one, because inventing an expectation would manufacture a gap that
nobody actually left.

\paragraph{Automated reporting.} One standardised report assembles context, triage, POCUS
findings, quantitative measures, values with their reference ranges, the reasoning, the
decision support and the safety envelope, closing with a conclusion and an archived copy
carrying a content hash.

Two choices in it are safety decisions rather than presentation ones. \textsc{not assessed} is
a section of the report rather than an omission: leaving it out would ask a reader to infer
absence from silence, and silence is what a normal result looks like. And a withheld
differential is rendered \emph{as withheld}, with its reason, because a report that merely
lacks a differential is indistinguishable from one where the model was never consulted.

The conclusion paragraph is assembled by template rather than generated. It is the line a busy
reader will act on, and it must say the same thing as the body; generating it would introduce
a place where the two could differ.

\subsection{Limitations}
\label{subsec:reasoning-limits}

The safety layer behaved as specified on five designed cases and on 335 automated tests.
That is the whole of what has been shown. The reasoning quality of the local model remained
limited, and the system was not evaluated against clinical ground truth: there is no
reference standard here saying what the correct differential was for any case. The five
cases were written to exercise particular failure modes, not sampled from a population, so
no rate of anything can be inferred from them.

The decision-support outputs of
Section~\ref{subsec:reasoning-decision-support} inherit that bound and add one of their own.
Their thresholds are conventional emergency-medicine warning bounds rather than values taken
from a validated scoring system, so an alert indicates that a configured limit was crossed and
nothing more. A deployment would replace that configuration with the escalation policy of the
institution using it.

This is decision support and not a diagnostic device. It has not been validated against
patient outcomes and is not suitable for clinical use.
